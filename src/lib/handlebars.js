module.exports = {
    eqS: function (a, b, v , options) {
        console.log("a en hele0r "+a);
        console.log("b in helper "+b);
        console.log("v in helper "+v);

        if (a == b) {
            return true;
        } else {
            return false;
        }
    }
};