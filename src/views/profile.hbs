<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card text-center">
                <div class="card-body">
                    <h3>Bienvenido {{user.fullname}}</h3> {{!-- para obtener el usr no necesecitamos hacer una consulta, porque lo tenemos almacenado en la sesion(serializado de passport) --}}
                    <h4>{{user.username}}</h4>
                    <a href="/" class="btn btn-primary m-4"> {{!-- para que el enlace parezca un boton... btn para que luzca como un boton, m-4 : margin 4 --}}
                        Comienza a utilizar la App.
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>